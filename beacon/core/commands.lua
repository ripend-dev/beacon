local kah, success = loadstring(game:HttpGet'https://raw.githubusercontent.com/szcvak/lua/main/Kohl\'s%20Admin%20House/Stellar/API.lua')()({
    ScriptName = 'beacon',
    Prefix = '#'
})

local cmds = {}

cmds.goto = {
    description = 'Walks to the targeted position',
    command = function(pos)
        
    end
}